<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <title>RecsWithParks</title>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>

  <body class="content-splash">
      <!-- Small Navigation -->
      <div class="title-bar transparent">
        <%= link_to image_tag("http://placehold.it/50x50/6ea53b/ffffff?text=RWP"), root_path, class: "logo-small show-for-small-only" %>
      </div>
      <!-- Medium-Up Navigation -->
      <nav class="top-bar transparent" id="nav-menu">
        <div class="logo-wrapper hide-for-small-only">
          <div class="logo">
            <%= link_to image_tag("http://placehold.it/350x150/6ea53b/ffffff?text=RWP"), root_path %>
          </div>
        </div>
        <!-- Left Nav Section -->
        <div class="top-bar-left">
          <ul class="menu">
            <li>
              <%= form_tag search_parks_path(:query), method: 'GET', id: 'searchBar'  do  %>
                <%= text_field_tag :query, "", placeholder: 'Search...', id: "query" %>
                <%= submit_tag 'Search', id: "qaSearchButton" %>
              <% end %>
              <a href="#" id="searchToggle" style="display: none;">Search</a>
            </li>
            <li>
              <%= link_to "Parks", parks_path %>
            </li>
          </ul>
        </div>
        <!-- Right Nav Section -->
        <div class="top-bar-right">
          <ul class="menu">
            <% if user_signed_in? %>
            <li><img src="<%= current_user.avatar.url %>" height="25" width="25"></li>
            <li><%= link_to current_user.email, user_path(current_user) %></li>
            <li><%= link_to 'Sign Out', destroy_user_session_path, method: 'DELETE', confirm: 'You sure?' %></li>
            <% else %>
            <li><%= link_to 'Sign in', new_user_session_path %></li>
            <li><%= link_to 'Sign up', new_user_registration_path %></li>
            <% end %>
          </ul>
        </div>
      </nav>
      <div class="container">
        <div class="row">
          <div class="column">
            <br>
            <div class="not-transparent panel callout">
            <div data-alert class="alert-box success"><%= notice %></div>
            <div class="alert"><%= alert %></div>
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
