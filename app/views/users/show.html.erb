<h1> <%= @user.email %> </h1>

<div>
  <img src="<%= @user.avatar.url %>" height="200" width="200">
</div>

<div>
  <h3>Your Reviews</h3>
  <% @reviews.each do |review| %>
    <li>Park Name: <%= review.park.name %><br>
    Review: <%= review.body %></li>
  <% end %>
</div>
